# 2026-03-17 작업 내역

## 완료된 작업

### 1. 디지털교실 빈화면/번역 버그 수정 (`0d6578c`)
- **번역 키 불일치 수정**: `digitalSchool.sections.*.step*.description` → `.desc` (27곳)
  - JSON은 `desc`인데 코드는 `description`을 참조하여 키 문자열이 그대로 노출되던 문제
  - `periods.*`는 JSON이 `description`을 사용하므로 그대로 유지
- **Suspense 추가**: `DigitalSchoolLayout.tsx`에서 `<Outlet />`을 `<Suspense>` + 스피너로 래핑
  - lazy-loaded 탭 컴포넌트 로딩 중 빈 화면 방지
- **로딩 스피너 추가**: 3개 탭 모두 `return null` → Loader2 스피너로 교체
  - `DigitalAttendanceTab.tsx`: `!user || schoolLoading || !progress` 통합
  - `DigitalCurriculumTab.tsx`: `isLoading` 체크 추가
  - `DigitalLabTab.tsx`: `isLoading` 체크 추가

### 2. 시간표 탭 UI 리디자인 (`2c8c8af`)
- **디지털교실 + 마케팅교실** 둘 다 적용
- 기존: 9개 섹션 × 3개 step = 27개 항목 전부 펼쳐서 세로 나열
- 변경:
  - **1일차/2일차 슬라이딩 탭**: 한 번에 4~5개 섹션만 표시
  - **아코디언**: 기본 접힘, 현재 진행 중인 교시만 자동 펼침 + 하이라이트
  - **상단 진행률 바**: N/6 완료 뱃지 + 프로그레스 바
  - 완료 교시: 접힌 상태에서도 ✅ 표시
  - 잠금 교시(졸업과제/졸업식): 흐리게 표시
- 수정 파일:
  - `src/pages/digital/school/DigitalCurriculumTab.tsx`
  - `src/pages/marketing/school/CurriculumTab.tsx`

### 3. Kiosk → Kkakdugi 리네이밍 완료 (`fa925f9`)
- 이전 커밋에서 파일만 rename되고 내용(import/export/변수명)이 안 바뀐 문제 수정
- 67개 파일의 내부 참조를 전부 Kkakdugi로 통일
- 구 `KioskSimulator/` 디렉토리 완전 삭제 확인
- src/, config 파일에 Kiosk 잔여 참조 0건 확인

### 4. 앱 아이콘 교체 (`9f5b848`)
- `public/favicon.svg`: "KR STARTER PACK" 파란 배경 → 빨간 깍두기 컬러 + 학교 건물 아이콘
- PWA 아이콘 3종 생성: `icons/icon-192x192.png`, `icon-512x512.png`, `apple-touch-icon.png`

### 5. 실습실 탭 레이아웃 개선 (`9f5b848`)
- "한국 생활 필수 앱" 카드를 키오스크 섹션 위로 이동
- 그라데이션 배경, 두꺼운 테두리, 큰 아이콘, "필수" 뱃지, 앱 이모지 미리보기 추가

### 6. 키오스크 8종 한국어 번역 추가 (`303d0a1`)
- `kkakdugi.types.*` 번역 키가 JSON에 없어서 영어 ID가 노출되던 문제
- ko: 카페, 패스트푸드, 병원, 은행, 주민센터, 영화관, 편의점, 공항
- en: Cafe, Fast Food, Hospital, Bank, Gov Office, Cinema, Conv. Store, Airport

---

## 미완료 / 다음에 할 작업

### 1. 반응형 레이아웃 최적화 (요청됨, 미착수)
- 사용자 피드백: "어떤 페이지는 최적화인데 어떤 페이지는 그렇지 않음"
- 모든 페이지가 모바일/태블릿/데스크톱에서 최적으로 보이도록 레이아웃 점검 필요
- 계획 모드로 진행 예정

### 2. PWA 캐시 문제
- 사용자가 모바일에서 옛 버전("seven" 문구 등)이 보인다고 보고
- GatewayPage 코드에는 "seven" 없음 — PWA 서비스 워커 캐시가 원인
- 서비스 워커 업데이트 전략 개선 검토 필요 (skipWaiting, 캐시 버스팅 등)

### 3. HTML 프로토타입 파일 정리
- `curriculum-redesign-preview.html`, `marketing-curriculum-redesign-preview.html` 삭제 필요
- 개발 참고용으로만 사용, 배포에는 포함되지 않지만 레포에 불필요

### 4. 기타 unstaged 변경사항
- 다수의 작업내역 .md 파일, BRANDING_PROGRESS.md, digital-restructure-plan.html 등
- `src/components/app-card/DownloadButton.tsx`, `src/components/digital/PracticeSection.tsx` 등 일부 src 파일
- `src/services/gemini/geminiClient.ts`, `marketCompassService.ts` 변경
- 필요한 것만 선별 커밋 or 일괄 커밋 결정 필요

---

## 확인된 정상 항목
- **필수 앱 다운로드 페이지**: 삭제되지 않음, `/track/digital-basics/korea-apps`에 건재
- **첫 페이지 브랜딩**: GatewayPage에 "깍두기 학교"로 정상 업데이트됨
- **리네이밍 완전성**: src/ 전체에 Kiosk 잔여 참조 0건
